apiVersion: v1
kind: Service
metadata:
  name: candlezone
  namespace: production
spec:
  selector:
    app: candlezone
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
