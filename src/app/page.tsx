import { siteConfig } from '@/config/site';

export const metadata = {
  title: `Stock Research & Analysis | ${siteConfig.name}`,
};

export default function Homepage() {
  return <main>New Homepage coming soon...</main>;
}
